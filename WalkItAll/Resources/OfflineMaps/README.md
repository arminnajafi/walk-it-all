# Offline maps

`manhattan-v2.sqlite` is generated by `Tools/CityPackBuilder` and bundled with the app. It contains public OpenStreetMap-derived geometry only. It must never contain workout routes or other user data.
